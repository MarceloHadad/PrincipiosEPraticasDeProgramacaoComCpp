#include "std_lib_facilities.h"

// Tente compilar aqueles exemplos e veja como o compilador responde.
int main()
{
	int area(int lenght, int width);

	int s1 = area(7;
	int s2 = area(7)
	Int s3 = area(7);
	int s4 = area('7);

	return 0;
}